apiVersion: v1
kind: Service
metadata:
  name: accommodation-2306275600-be-service
  namespace: default
spec:
  selector:
    app: accommodation-2306275600-be
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080